# Open Food Facts requires a custom User-Agent (configured in lib/open-food-facts/client.ts).
# No API key is needed for search.

# Groq — AI Safe-Recipe Generator (app/actions/recipe.ts)
GROQ_API_KEY=
